#pragma once
/*
 * config.h only exists to make the gnulib code work.
 * The real config lives in sandboxconfig.h.
 */
#ifdef HAVE_CONFIG_H
#include <sandboxconfig.h>
#endif
#include "libsbutil/gnulib/glue.h"
